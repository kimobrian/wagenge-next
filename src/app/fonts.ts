import { Inter, Roboto_Mono, Eczar, Andika } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const roboto_mono = Roboto_Mono({
  subsets: ["latin"],
  display: "swap",
});

export const eczar = Eczar({
  subsets: ["latin"],
  weight: ["400", "500", "700"],
  display: "swap",
});

export const andika = Andika({
  subsets: ["latin"],
  weight: ["400", "700"],
  display: "swap",
});
